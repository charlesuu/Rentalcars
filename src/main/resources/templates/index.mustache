{{>layout/header}}

<h1>RentalCars 차량 렌트 서비스</h1>
<div class="col-md-12">
    <div class="row">
        <div class="col-md-6">
            <a href="/user/save" role="button" class="btn btn-primary">사용자 등록</a>
            <a href="/car/save" class="btn btn-success active" role="button">차량 등록</a>
        </div>
    </div>
    <br>
    <!-- 목록 출력 영역 -->
    <table class="table table-horizontal table-bordered text-center">
        <thead class="thead-strong">
        <tr>
            <th style="width: 10%">유저 번호</th>
            <th style="width: 25%">이름</th>
            <th style="width: 20%">나이</th>
            <th style="width: 20%">사용자 수정, 삭제</th>
            <th style="width: 20%">렌탈 관리</th>
        </tr>
        </thead>
        <tbody id="tbody">
        {{#users}}
            <tr>
                <td>{{id}}</td>
                <td>{{name}}</td>
                <td>{{age}}</td>
                <td><a href="/user/update/{{id}}" class="btn btn-info active" role="button"> 수정, 삭제 </a></td>
                <td><a href="/car/rental/{{id}}" class="btn btn-secondary active" role="button"> 렌트, 반납 </a></td>
            </tr>
        {{/users}}
        </tbody>
    </table>
</div>

{{>layout/footer}}